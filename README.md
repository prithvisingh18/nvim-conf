# For quick setup using LazyVim
```sh
mkdir ~/.config/nvim
cd ~/.config/nvim
git clone https://github.com/LazyVim/LazyVim
```
# For quick setup using this repo
```sh
mkdir ~/.config/nvim
cd ~/.config/nvim
git clone git@github.com:prithvisingh18/nvim-conf.git
```

